{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy": "npm run docs:build && node scripts/deploy.js"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.12"
  }
}
